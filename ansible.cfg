[defaults]
# WARNING: host_key_checking is disabled here to support the bootstrap workflow
# (first-time key deployment to unconfigured hosts). For production automation
# against already-configured hosts, set this to True or remove this line.
host_key_checking = False
inventory = inventories/hosts.yml
roles_path = roles

[ssh_connection]
pipelining = True